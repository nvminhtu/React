// @flow

class Media {
  name: string
  static defaultMedia() {
    return new string();
  }
}
class Movie extends Entertainment {

  constructor(price) {
    super(price);
  }

  constructor(name: string) {
    this.name = name
  }

  show() {
    return `Begin ${this.name}`
  }
}

export default Movie
